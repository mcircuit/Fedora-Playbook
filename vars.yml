---
#run whoami command to find username
target_user: "merlin"

target_home: "/home/{{ target_user }}"

#run hostname command to find hostname
allowed_hostnames:
  - merlin-pc       # laptop
  - localhost
  - merlin
